Version 0.3.1

Bug Fixes:
- Fixed a bug where clouds have collision
- Fixed a bug where shrubs have collision
- Fixed a bug where the music wouldn't change when a level was loaded in

Additions and Changes:

- Added UUIDs to all Elements
- Layered tiles calculations are now stored
- Layered tiles are only calculated now when they have not been stored
- Card images are no longer calculated every tick but are now stored
- Backgrounds are now stored instead of retrieved every frame\
- Removed custom entity support (was non functional to begin with. Will be re-added later)
